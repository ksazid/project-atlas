# VS-16 Expo runtime evidence — 2026-08-10

## Evidence status

This record preserves implementation and authentic Expo Web runtime verification for VS-16 — URL-First Business Discovery & Owner Confirmation at implementation head `c8a3fa82dda624f555f63e47c598501bad86cfc7`.

The verification executed on the GitHub-hosted Ubuntu runner in CI run `31338865481`. The run exported the actual `apps/mobile` Expo Router application, launched runner-provided Google Chrome headlessly, exercised the real `/create-business` discovery/confirmation journey at phone and tablet viewports, re-ran the certified VS-15 `/context` journey, applied the PostgreSQL migration, built the .NET API, ran API/mobile tests, and uploaded the `pes-dashboard` evidence artifact.

Subsequent certification close-out commits are governance/documentation-only and must pass their own exact-head CI, Security baseline, and Product Intake gates before merge. This evidence does not authorize deployment, release, or production enablement.

## Runtime boundary

The VS-16 verifier is `tests/mobile/business-discovery-runtime.test.mjs`.

It uses deterministic development fixtures only at application boundaries:

- authenticated browser session is supplied through the existing temporary web-session shim;
- a local fixture implements `POST /api/v1/business-discovery` and `POST /api/v1/businesses/from-discovery`;
- discovered values are fixture-backed public observations (`Harbour Coffee`), not screen-level hard-coded Starbucks/demo business facts;
- timezone and currency are intentionally absent from discovery so the runtime proves missing values remain unknown and are owner-completed;
- every exercised request must use `Bearer runtime-token`;
- the exact persisted discovery snapshot is consumed once to create the Business.

The existing VS-15 Context runtime is also executed in the same mobile gate. Its Expo export now clears the transform cache so independent runtime fixtures cannot accidentally share `EXPO_PUBLIC_API_URL`.

## VS-16 runtime assertions

The passing run verified:

- URL-first discovery reaches the discovery API boundary;
- only observed fixture facts are presented;
- unavailable timezone/currency remain unknown rather than being silently inferred;
- the owner is asked to complete only missing required details;
- confirmation consumes the exact discovery snapshot;
- owner confirmation reaches Business creation;
- the authenticated API boundary is preserved;
- 390×844 and 768×1024 layouts have no horizontal overflow;
- the primary action measures at least 44 CSS pixels.

The runtime recorded one discovery call and one Business-creation call.

## Captured viewports

The `pes-dashboard` artifact `9045216751` from CI run `31338865481` contains `runtime-vs16/runtime-summary.json` and these authentic Chrome captures:

| Viewport/state | Artifact file | SHA-256 |
| --- | --- | --- |
| 390×844 confirmation | `runtime-vs16/discovery-390x844-confirm.png` | `29304d5eba62ad915b466eddd6b357aa31913103e06eeb4720cfd816fd2cf6d6` |
| 390×844 missing details | `runtime-vs16/discovery-390x844-missing-details.png` | `c633fba2e20df13d670bec593a87719a06b364bfb94abacd2f392b96c666bee0` |
| 768×1024 confirmation | `runtime-vs16/discovery-768x1024-confirm.png` | `b4674e7f84022f03780f7ca48503ee5ed752c57f17078006ce81dace05bf3fa9` |

The artifact digest is `sha256:0723f15429aa017e577ceb03121f445dd8c114669ce4e7481b3d00c0d64f6ef1` and GitHub records its head SHA as `c8a3fa82dda624f555f63e47c598501bad86cfc7`.

## Existing Context regression evidence

The same exact-head preflight also re-executed the authentic VS-15 Context runtime at 390×844 and 768×1024. It passed loading, recoverable error, retry, provenance confirmation, validation, draft-safe save failure, saving/success, authenticated Business isolation, overflow, and minimum-target assertions.

The cross-runtime CI investigation identified Expo transform-cache reuse between independently configured runtime fixtures. The final implementation keeps the certified Context behavior unchanged and isolates the Context export with `expo export --clear`; the speculative Context state-order change was removed before this evidence run.

## Deterministic gates

All required implementation-head gates passed on `c8a3fa82dda624f555f63e47c598501bad86cfc7`:

- CI run `31338865481` — success: planning/governance checks, TypeScript, Expo lint, complete mobile tests, both authentic Expo Web runtimes, .NET restore/build, EF migration application, API tests, dashboard build and artifact upload;
- Security baseline run `31338865479` — success;
- Product Intake run `31338865478` — success.

## Visual evidence

The retained confirmation captures show the approved Atlas V2 continuity: clean white canvas, deep-green editorial hierarchy, restrained mint/public-evidence surfaces, rounded but compact cards, explicit provenance/confidence labels, and owner-controlled missing-detail completion. The runtime contains no fabricated Starbucks ratings, reviews, phone numbers, opening hours, or Malta defaults.

`BrandMark` remains the single replaceable temporary mark boundary; no production identity decision is made by this slice.

## Certification boundary

VS-16 certification is bound to tested implementation head `c8a3fa82dda624f555f63e47c598501bad86cfc7`.

Certification/governance commits after that SHA may be merged only if their final PR head passes CI, Security baseline, and Product Intake. Release and production approvals remain pending/not authorized.

## Limitations

Expo Web/headless Chrome evidence does not prove native iOS VoiceOver traversal, UIKit dynamic type, physical touch behavior, or native keyboard behavior. Those remain release-level checks. No EAS build, OTA update, deployment, production endpoint, production credential, or external publishing action was used.
