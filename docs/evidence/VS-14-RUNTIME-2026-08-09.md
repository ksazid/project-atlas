# VS-14 Expo runtime evidence — 2026-08-09

## Evidence status

This is authentic Expo Web implementation/testing evidence for VS-14 Goals Visual Migration at application commit `02810355c16e1f63c036f1a526321142b8d85ef1`. It does not certify a commit, authorize release or production, or replace exact-SHA CI, Security, Product Intake, native-device, and human approval gates.

## Runtime boundary

Expo served the actual `apps/mobile` application and bundled `expo-router/entry`. An external local development fixture implemented the unchanged `GET`/`PUT /api/v1/businesses/dev-business/goals` contract. Browser storage was seeded through a temporary Expo SecureStore Web shim with `atlas.access-token=runtime-token` and `atlas.business-id=dev-business`; the shim was restored after capture. The fixture and all data were local, development-only, and did not change the API, DTO, auth, database, navigation, dependencies, production configuration, or release state.

## Routes, viewports, and assertions

| Route | Viewport | Evidence | Runtime result |
| --- | --- | --- | --- |
| `/goals` | 390×844 touch | `vs-14/goals-390x844-ready.png` | Delayed load visibly rendered `Loading your goals`; priority-ordered server goals rendered after recovery. Move actions measured 44×44px; Save measured 52px high. |
| `/goals` | 390×844 touch | `vs-14/goals-390x844-custom-validation.png` | Empty and duplicate custom-goal validation used stable copy and retained the typed draft. Narrow document width was 390px with no horizontal overflow. |
| `/goals` | 390×844 touch | `vs-14/goals-390x844-save-failure.png` | Controlled PUT failure displayed `Could not save goals. Your priorities are still here.`, kept the custom goal visible, and re-enabled Save for retry. |
| `/goals` | 390×844 touch | `vs-14/goals-390x844-save-success.png` | A delayed PUT visibly rendered `Saving…`, dynamic accessible name, `aria-busy=true`, and disabled state; the returned server goals rendered after successful retry and `Goals saved.` announced in the polite live region. |
| `/goals` | 768×1024 | `vs-14/goals-768x1024-ready.png` | Responsive contained layout rendered with document width 768px and no horizontal overflow. |

The same run also asserted missing-session recovery at 390×844: `No business selected` exposed a 44px `Choose or create a business` action that reached `/create-business`. An empty goals response rendered the explicit unsaved starter-goals notice. The initial server response was sorted by priority, and all GET/PUT requests used `Bearer runtime-token` with the authenticated `dev-business` path only. Welcome, Profile, and the five-item bottom navigation were revisited to check visual/navigation continuity.

## Visual review

| Before | After | Why |
| --- | --- | --- |
| Existing VS-14 Goals rendering | No visual code change | Authentic phone/tablet review found coherent warm canvas, green hierarchy, card geometry, and bounded press/disabled feedback; no polish change was justified. |

## Limitations

The Linux workspace has no Xcode, iOS Simulator, connected physical device, or native automation channel. Expo Web does not prove UIKit dynamic type, native screen-reader traversal, physical touch behavior, or native keyboard inset behavior. The temporary remote BrandMark host was intentionally blocked in the capture sandbox, so the mark may appear blank; this is an environment result and the known pre-production local/failure-fallback requirement remains open. No production endpoint, data, deploy, OTA update, or release action was used.

## Whole-branch final-fix verification

Application/test commit `1d57970d2b17a0c80055dd15cf62068be75510d7` fixes the two whole-branch review findings. Focused real-behavior tests verify that loaded and server-confirmed priorities remain unchanged through stable ordering, boundary moves preserve the input representation, valid owner moves/additions normalize priorities, and the production-consumed operation coordinator rejects overlapping Retry/Save operations and stale completion tickets.

An additional authentic Expo Web run at 390×844 rendered the actual Goals load-error route and its enabled Retry and Save actions. The sandboxed browser request did not reach the temporary delayed-response fixture, so the required delayed Retry→blocked Save→confirmed Save sequence was inconclusive in that run. No new screenshot is preserved and no authentic interaction pass is claimed for that supplemental check. The deterministic regression coverage and the earlier authentic VS-14 screenshots remain distinct evidence; native and exact-head remote gates remain pending.

## Preserved file integrity

| File | SHA-256 |
| --- | --- |
| `vs-14/goals-390x844-custom-validation.png` | `bbdc55f37426cc61f9a826e045b1ac7916cc842f1d0ae2f787ab7b194ad790ce` |
| `vs-14/goals-390x844-ready.png` | `afe9cd4e6a900ef1f78a4c1a9f249423e55d327d4e9557373c184a3d77354518` |
| `vs-14/goals-390x844-save-failure.png` | `d034df95cacf944bd08532dfa9256da6beb54440d4b3940f5154048199eb832d` |
| `vs-14/goals-390x844-save-success.png` | `4a2fd17ddec1639eb317c37bc8605c7b063aed262864bb645ec780105047e34b` |
| `vs-14/goals-768x1024-ready.png` | `94487a26c147808195af09f0f3a02f12b850db03f431ea6fb8763ccf8168cfea` |
