# VS-31 — Native Navigation Alignment

## Status

Certified runtime slice. Exact implementation SHA: `2926727cbc1b25c5eb29853240e208636cc20253`.

## Requirements

- FR-03 — Business Profile
- FR-12 — History
- FR-16 — Empty and degraded states

## Goal

Resolve the long-standing mismatch between Atlas's approved four-destination navigation and the inherited five-tab runtime without removing Business, Context or Settings capabilities.

## Delivered persistent navigation

1. Today
2. History
3. Goals
4. Profile

Business Hub remains the Profile root. Context and Settings are pushed Profile-detail Stack routes. History is a true tab root while retaining the `/history` path.

## Delivered runtime scope

- retained Expo Router NativeTabs from VS-28;
- aligned persistent tab labels/routes to Today / History / Goals / Profile;
- moved History into the tab route group while preserving `/history`;
- moved Context and Settings out of the tab group into root Stack detail routes while preserving `/context` and `/settings`;
- added Profile access to Context and Settings;
- added visible accessible Back-to-Profile native-header affordances with deep-link fallback;
- removed the obsolete Back action from History;
- added Android History fallback icon;
- made AtlasScreen reserve tab spacing only for routes actually inside `(tabs)` and defer top-safe-area ownership to native headers for Context/Settings;
- preserved existing Business Hub, Context, Settings, reset, notification, History and Weekly Review behavior;
- introduced no API, database, migration, auth, recommendation, release or deployment changes.

## Out of scope preserved

- API/backend/database changes;
- migrations;
- Business Hub workflow redesign;
- Context workflow redesign;
- Settings/reset redesign;
- auth changes;
- Today’s Focus/recommendation changes;
- deployment/release/EAS/OTA/production enablement.

## Decision

DEC-10 resolves the temporary navigation deferrals in DEC-02, DEC-03 and DEC-09 for future runtime work. Historical decisions remain preserved.

## TDD and certification evidence

- Activation baseline `298731e73220a54c51674ab7d5b39769522a4a70`: CI `31568707702`, Security `31568707731`, Product Intake `31568707678` all green before runtime edits.
- RED `8717e24b558a4c644abf4152fa7f3f885c9f8828`: exactly five intended VS-31 navigation failures reproduced while 152 existing mobile tests passed.
- Frozen GREEN runtime `2926727cbc1b25c5eb29853240e208636cc20253`:
  - CI `31580555641` — success;
  - mobile — 158 passed, 0 failed;
  - TypeScript — success;
  - Expo lint — success;
  - authentic Expo Web runtime checks — success;
  - PostgreSQL 17 migration replay — success;
  - API — 325 passed, 0 failed, 0 skipped;
  - dashboard build/artifact upload — success;
  - Security baseline `31580555710` — success;
  - Product Intake `31580555630` — success.
- Changed-file review confirmed no API, migration, infrastructure, release or deployment files changed.

## Release boundary

Certification does not authorize production release, deployment, EAS build/submit/OTA, production enablement, or production database mutation.