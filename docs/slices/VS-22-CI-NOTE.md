# VS-22 stacked CI note

VS-22 is implemented on `atlas/vs22-multi-source-discovery`, currently stacked on `atlas/expo-go-test-harness` because VS-21 has not yet been merged to `main`.

A Draft PR may target the VS-21 working branch during implementation only to obtain exact-head CI for TDD. This does not authorize merge, release, deployment-branch modification or production deployment.

Before any merge discussion, the branch/base must be reconciled with the final VS-21 outcome and the exact resulting head must be re-certified.
